using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Movement : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Transform[] joint = new Transform[5];
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
